# Anti-slop 적용 기록

## 인터넷 원문 선정

주 기준은 [miqdadbadjuber/anti-slop](https://github.com/miqdadbadjuber/anti-slop)의 `antislop.md`다. UI 기법을 일괄 금지하지 않고, 각 기법이 정보 위계·정체성·가독성에 기여하는지 설명하도록 요구한다. MIT 라이선스와 원문 파일을 확인한 뒤 커밋 `f5e8ccf1bbf9fbef4ce7e15b60459eb17c6a6f58`에 고정해 프로젝트 루트에 보관했다.

[funboy322/avoid-ai-design](https://github.com/funboy322/avoid-ai-design)의 `SKILL.md`도 교차 확인했다. 코드와 실제 픽셀을 함께 보고, 제품 고유 방향을 먼저 정한 뒤 기능·접근성을 보존하면서 수정하라는 원칙을 적용했다.

## 이 프로젝트의 방향

방향은 `DESIGN.md`에 분리했다. AIDAS 화면은 일반 웹 대시보드가 아니라 촬영 장면 속 밝은 운용 콘솔이다. 실제 영상 3개, 현재 작업, 작업 단계가 한 화면에서 연결되는 것이 목적이다.

- ENERGY 1: 장식보다 정보가 앞서는 차분한 화면
- RHYTHM 2: 큰 카메라 하나와 작은 카메라 두 개의 비대칭 위계
- MOTION 1: 상태 전환을 설명하는 최소 전환만 사용

## 적용한 변경

- 전체 배경과 패널의 그라데이션과 글로우를 제거했다.
- 참고 사진의 청색 편이를 복제하지 않고 중성 회색 작업면, 흰색 패널, 진한 회색 본문으로 바꿨다.
- 참고 화면의 운영 도구 밀도는 좌측 작업 단계, 카메라 제목 레일, 실제 장면 타임라인으로 재해석했다.
- 카메라 대기 화면의 격자, 중앙 선, 네 모서리 리티클을 제거했다.
- 정보가 없는 원형 AI 오브와 `FEATURED VIEW / PRIMARY DISPLAY` 장식을 제거했다.
- 상태색은 활성 단계, 현재 작업 레일, 상태명으로 제한했다. 카메라 주 화면 테두리는 고정 파랑으로 분리했다.
- 카메라 제목 레일을 영상 밖으로 옮기고 영상에는 색상 필터를 적용하지 않았다. 작업 부위가 잘리지 않도록 `object-fit: contain`을 사용했다.
- 영문 대문자 중심 문구를 짧은 한국어로 바꿨다. 영문은 프로토콜과 `CAM 01` 같은 식별자에만 남겼다.
- 출처 없는 수치와 성능 주장을 추가하지 않았다. 카메라 위치 이름은 화면 초안의 제안임을 시나리오 문서에 유지했다.
- 보이는 카메라 타일을 키보드로 전환할 수 있게 하고, 포커스 표시를 추가했다.
- 미연결, 연결 중, 실시간, 오류 상태와 `C`, `Escape`, 방향키 동작을 유지했다.

## 목적이 있어 유지한 요소

- 중성 회색 작업면과 흰 패널: 밝은 운용 도구의 인상을 만들면서 촬영된 참고 사진의 청색 편이를 피한다.
- 왼쪽 6단계 목록: 현재 작업의 위치를 실제 시나리오 구조 안에서 보여준다.
- 상태별 색: 안전 정지, 실패, 시연, 완료를 언어 외 수단으로 구분한다. 한 화면에는 하나만 활성화된다.
- 카메라 제목 레일: 영상 내용을 가리지 않고 카메라 이름과 연결 상태를 읽게 한다.
- 실제 장면 타임라인: 참고 화면의 편집기 밀도를 기능 없는 장식이 아니라 시나리오 이동 조작으로 바꾼다.
- 제한된 그림자: 흰 화면에서 상단 바와 열린 입력 설정 패널의 높이만 구분한다.

## 검증

- 원문 무결성: 로컬 `antislop.md`와 고정 커밋 원문의 SHA-256이 일치함
- ESLint: 통과
- 프로덕션 빌드: 통과
- 1920×1080: 가로·세로 넘침 없음, 카메라 3개와 작업 단계·현재 작업이 첫 화면 안에 유지됨
- 1024×720: 가로·세로 넘침 없음, 핵심 문구 27px, 설명과 카메라명이 16px 이상으로 유지됨
- 390×844: 가로 넘침 없음, 세로 흐름으로 전환되며 주요 버튼 높이 44px 이상
- 색 대비: 일반 본문과 안내문 중 가장 낮은 확인값 4.92:1, 상태색은 흰 패널에서 5.35:1 이상
- 시각 기법: 그라데이션·글로우·배경 격자·영상 색상 필터 없음, 그림자는 상단 바와 열린 입력 설정 패널에만 사용
- 키보드: 방향키 장면 전환, Enter 카메라 승격, `C` 패널 열기·닫기, `Escape` 닫기 확인
- 상호작용: 주 카메라 클릭·숫자 단축키, 이전·다음, 장면 상태 선택, 입력 설정 열기·닫기 확인
- 브라우저 콘솔: 경고·오류 없음

실제 웹캠 권한 요청과 3대 동시 연결은 이 검증에서 실행하지 않았다.
