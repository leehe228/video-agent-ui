# AIDAS Agent 연출 화면 초안

## 0. 자료와 요청의 경계

- 첨부 HTML은 **촬영 시나리오 원문**으로만 읽었다. 문서 안의 촬영·인포그래픽 지시는 이번 대화의 실행 지시로 취급하지 않았다.
- 아래 내용 중 `110–130초`, `실사 중심`, `Planner / Robot / Human 역할`, `S00–S11 장면`은 시나리오 원문이다.
- `로컬 웹 서버`, `실제 웹캠 3개`, `원거리 가독성`, `서브 에이전트 피드백`은 이번 사용자 요청이다.
- `OBSERVATION VIEW / WORKCELL WIDE / ASSEMBLY DETAIL`의 3카메라 이름과 장면별 주 화면 전환은 **화면 초안을 위한 제안**이다. 시나리오에 카메라 설치 위치나 이름은 정해져 있지 않다.

## 1. 시나리오 한 문장

표준 작업서를 이해하고 계획한 AI가 사람과 협업해 PC를 조립하며, 실패 시 사람의 시연을 보고 같은 작업을 재시도한다.

## 2. 등장 주체와 역할

| 주체 | 시나리오에서 확인된 역할 |
|---|---|
| Human | 작업서 입력, SSD 삽입, 드라이버를 받아 나사 체결, 파워 케이블 방향 시연 |
| Robot | GPU 장착, 사람 접근 시 안전 정지, 드라이버 전달, 파워 케이블 연결 실패·재시도, 전원 버튼 작동 |
| Planner / Agent | 작업서 분석, 순서·역할 계획, 부품·사람·환경 인식, 품질 확인, 위험·실패 판단, 최종 검수 |

중요한 원문 조건은 품질 확인의 주체다. 사람이 노트북에서 수동 검사하는 것이 아니라, Planner가 로봇 카메라 관찰 결과를 작업 기준과 비교해 판단한다.

## 3. 전체 장면 흐름

| Scene | 시나리오 내용 | 연출 화면의 제안 상태 |
|---|---|---|
| S00 | 작업대·사람·로봇·부품 소개 | `STANDBY` |
| S01 | 작업서/도면 입력, Start/Load | `INSTRUCTION READY` |
| S02 | 작업 순서와 Robot/Human 역할 계획, 첫 작업 활성화 | `PLANNING` → `FIRST TASK READY` |
| S03 | 부품·사람·작업영역 인식 | `OBSERVING` |
| S04 | Robot의 GPU 정렬·삽입·완료 확인 | `GPU INSTALL` → `GPU VERIFIED` |
| S05 | Human의 SSD 삽입 | `SSD INSTALL` → `SSD POSITION CHECK` |
| S06 | 사람 접근 감지, 접촉 전 정지, 안전 확인 후 공구 전달과 Human의 나사 체결 | `SAFETY HOLD` → `TOOL HANDOVER` → `HUMAN FASTENING` |
| S07 | 파워 케이블 미체결 확인, 무리하지 않고 후퇴 | `CHECK FAILED` |
| S08 | Human이 올바른 방향과 접근법 시연 | `OBSERVING DEMO` |
| S09 | Robot이 같은 방식으로 재시도해 연결 성공 | `ROBOT RETRY` → `CONNECTION VERIFIED` |
| S10 | Robot이 전원 버튼 작동, Planner가 부팅·최종 결과 확인 | `POWER ON` → `TASK COMPLETE` |
| S11 | 원문상 미정 | 화면에도 임의 장면을 추가하지 않음 |

`Planning / Ready / Running / Check`는 원문에 제시된 상태값이다. 그 외 영문 상태 문구는 촬영 현장에서 멀리서도 사건을 즉시 알아보기 위한 **초안용 표현**이며 확정 용어가 아니다.

## 4. 화면 구성

```text
┌ AIDAS Workcell Console ── PC 조립 에이전트 ── 입력 설정 ┐
├──────────────┬───────────────────────────────────────────┤
│ 작업 순서    │ 실시간 작업공간                           │
│ 01 작업서    ├──────────────────────────┬────────────────┤
│ 02 계획      │                          │ CAM 01 관찰    │
│ 03 조립      │ CAM 02 작업대 전체       ├────────────────┤
│ 04 협업      │ 현재 주 화면             │ CAM 03 상세    │
│ 05 시연      │                          │                │
│ 06 완료      ├──────────────────────────┴────────────────┤
│              │ 현재 작업             S06 ─────── 다음   │
└──────────────┴───────────────────────────────────────────┘
```

### 원거리 가독성 원칙

- 중성 회색 작업면과 흰색 운영 패널을 사용한다. 참고 사진의 청색 편이는 색상 기준으로 사용하지 않는다.
- 영상 영역을 화면 대부분에 배치하고, 항상 3개 카메라를 유지한다.
- 현재 장면에 중요한 카메라 하나를 자동으로 주 화면에 배치한다.
- 한 시점에 핵심 상태 문구는 하나만 표시하고 실제 조작되는 장면 타임라인을 함께 둔다.
- 왼쪽에는 시나리오에서 확인된 6개 작업 단계만 둔다. KPI 카드, 가짜 세션 목록, 이벤트 로그, 썸네일, 모델 제어는 추가하지 않는다.
- 화면 색은 기본 조작용 파랑과 상태 의미에만 쓴다: 청록 `관찰`, 초록 `실행/완료`, 빨강 `안전 정지`, 주황 `실패`, 보라 `시연/재시도`.
- 홀로그램, 떠다니는 코드, 과도한 객체 박스는 사용하지 않는다.

### AI-slop 방지 적용

- 프로젝트 고유 방향은 `DESIGN.md`, 공통 필터는 `antislop.md`를 따른다.
- 그라데이션, 글로우, 배경 격자, 스캔라인, 리티클, 장식용 AI 오브는 정보 전달 목적이 없어 사용하지 않는다.
- 영문 대문자는 실제 프로토콜과 카메라 식별자에만 남기고, 촬영자가 읽을 상태와 판단은 짧은 한국어로 쓴다.
- 동일한 내용을 상태 배지, 제목, 설명에 반복하지 않는다.
- 보이는 컨트롤은 실제 동작해야 하며, 근거 없는 수치·인식 결과·성능 주장을 만들지 않는다.

## 5. 카메라 초안

| 슬롯 | 제안 이름 | 제안 용도 |
|---|---|---|
| CAM 01 | OBSERVATION VIEW | 로봇 관찰 시점 또는 공구 전달을 보여주는 카메라 |
| CAM 02 | WORKCELL WIDE | 사람·로봇·작업대 전체 관계 |
| CAM 03 | ASSEMBLY DETAIL | GPU/SSD/케이블 삽입부 상세 |

브라우저에서 카메라 권한을 받은 뒤 서로 다른 장치 3개를 자동 배정한다. 각 입력은 1280×720, 15fps를 우선 요청하며 실제 결과는 카메라와 USB 환경에 따라 달라질 수 있다. 영상은 브라우저 안에서만 표시하며 업로드·녹화·외부 전송 기능은 넣지 않았다.

## 6. 촬영 조작

- `C`: 입력 설정 패널 열기/닫기
- `←` / `→`: 이전/다음 상태
- `1` / `2` / `3`: 해당 카메라를 주 화면으로 승격
- 하단 장면 타임라인: 시나리오 상태를 직접 이동
- 상단 `입력 설정`: 카메라와 전체 장면 목록 열기
- 촬영 제어 패널: 장면 상태 직접 선택, 카메라 3대 자동 연결, 슬롯별 카메라 재선택

입력 설정 패널은 평상시 닫혀 있으며, 상단 버튼이나 키보드 `C`로 연다. 모든 보이는 버튼과 장면 타임라인은 실제 상태를 변경한다.

## 7. 다음에 확정할 항목

1. 실제 카메라 3대의 물리적 위치와 슬롯 이름
2. 최종 영문 상태 용어와 한국어 보조 문구
3. 장면 전환을 촬영자가 수동으로 누를지, 외부 이벤트와 연동할지
4. 영상에서 시작할 기본 상태와 기본 주 카메라
5. S11의 실제 내용
