# AIDAS 촬영 화면 디자인 방향

## Design read

촬영 현장의 노트북과 모니터에 띄우는 PC 조립 에이전트 운용 화면으로 읽는다. 밝은 작업실에서 실제 운영자가 카메라 입력과 작업 단계를 다루는 데스크톱 콘솔의 시각 언어를 사용한다.

Dial: ENERGY 1 / RHYTHM 2 / MOTION 1

## 정체성과 분위기

- 정체성: 데모 장면 속에서 실제로 쓰이는 운용 화면
- 성격: 차분함, 정확함, 실용적임, 과장하지 않음
- 금지할 인상: SF HUD, 터미널 코스프레, 미래 기술 광고, 장식용 SaaS 대시보드, 참고 제품의 복제
- 화면 우선순위: 실제 영상 > 현재 작업 > 작업 단계 > 카메라 연결 상태 > 촬영 제어

## 구성

- 웹캠 3개를 항상 유지한다. 현재 장면의 핵심 카메라 하나는 크게, 나머지 두 개는 보조 화면으로 둔다.
- 왼쪽 작업 단계 패널, 중앙 카메라 작업공간, 하단 상태 타임라인으로 실제 운용 흐름을 드러낸다.
- 현재 판단은 영상보다 커지지 않는 범위에서 제목과 한 줄 근거로 표시한다.
- 카메라 선택과 전체 장면 목록은 상단 `입력 설정`으로 여는 운영 패널 안에 둔다.
- 모바일 페이지가 아니라 16:9 노트북·모니터용 촬영 화면이다. 기준 해상도는 1920×1080, 지원 하한은 1024×720이다.

## 색과 형태

- 기본은 흰색 패널, 옅은 중성 회색 작업면, 진한 회색 텍스트다. 촬영된 참고 이미지의 푸른 색편은 배제하고 중성 백색을 기준으로 한다.
- 기본 인터랙션은 절제된 로열 블루 하나로 통일한다. 안전 정지, 실패, 실행 완료처럼 의미가 달라야 하는 상태에서만 빨강, 주황, 초록을 보조로 쓴다.
- 여러 상태색은 동시에 장식으로 쓰지 않는다. 활성 단계선, 상태명, 타임라인 표시처럼 의미가 있는 위치에만 제한한다.
- 평면 패널과 4~6px 모서리를 사용한다. 패널 분리는 얇은 회색 선으로 하고, 그림자는 상단 바와 운영 패널의 높이 구분에만 제한한다.
- 그라데이션, 글로우, 배경 격자, 스캔라인, 리티클, AI 오브, 장식용 아이콘은 쓰지 않는다.

## 타이포그래피와 문구

- 한국어 시스템 산세리프를 사용한다. 촬영용 Mac에서 안정적으로 렌더링되고 운용 도구의 정보 밀도를 유지하면서도 원거리 판독성이 높기 때문이다.
- 본문과 컨트롤은 16px 이상, 패널 제목은 20px 이상, 현재 판단은 30px 이상을 기본으로 한다. 참고 화면처럼 작아 보이되 촬영 현장에서 읽히지 않는 크기로 줄이지 않는다.
- 한국어를 기본으로 쓰고 `READY`, `PLANNING`, `RUNNING`, `CHECK`, `CAM 01`처럼 실제 프로토콜이나 장치 식별자만 영문 대문자로 남긴다.
- 근거 없는 정확도, 객체 수, 신뢰도, 처리 속도, 성능 수치와 마케팅 문구를 만들지 않는다.
- 같은 의미를 상태, 배지, 제목, 설명에 반복하지 않는다.
- 시나리오에서 확정되지 않은 카메라 위치와 S11 내용은 제안 또는 미정으로 표시한다.

## 움직임과 상호작용

- 전환은 상태 변화가 어디에 반영됐는지 이해시키는 짧은 색상·위치 전환에만 사용한다.
- 장식용 부유, 펄스, 반복 발광, 스크롤 등장 효과는 사용하지 않는다.
- 보이는 버튼과 선택지는 모두 실제로 동작해야 한다. 카메라에는 미연결, 연결 중, 실시간, 오류 상태가 있어야 한다.
- 키보드 포커스는 분명히 표시하고 운영 패널은 `Escape`로 닫을 수 있어야 한다.

## 주요 결정과 이유

- 흰색 운영 콘솔: 참고 화면의 실제 운용 도구 인상을 가져오되 촬영으로 생긴 청색 편이는 복제하지 않기 위해서다.
- 좌측 단계 패널: 현재 장면이 전체 PC 조립 흐름에서 어디에 있는지 운영자가 바로 찾게 하기 위해서다.
- 비대칭 3카메라 배치: 세 영상을 유지하면서 현재 장면의 핵심 시점을 한눈에 구분하기 위해서다.
- 파란 선택선: 연결 상태와 구분되는 방식으로 현재 주 카메라와 현재 단계를 표시하기 위해서다.
- 하단 장면 타임라인: 참고 화면의 편집기 구조를 장식으로 흉내 내지 않고 실제 장면 이동 기능으로 바꾸기 위해서다.
- 제한된 그림자: 흰 화면에서 상단 바와 슬라이드 운영 패널의 높이만 구분하기 위해서다.
